{
  "name": "membership-management-tracker",
  "version": "1.0.0",
  "private": true,
  "description": "Membership Management Tracker - MERN app (MongoDB, Express, React, Node) for managing gym/studio memberships, plans, slots, pricing, and dashboards.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "lint": "echo \"No root lint configured\"",
    "start": "npm run start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
